def bubble_sort(arr)
  n = arr.length

  for i in 0..n-2 do
    for j in 0..n-i-2 do
      if arr[j] > arr[j+1]
        arr[j], arr[j+1] = arr[j+1], arr[j]
      end
    end
  end

  return arr
end

arr = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5]

sort_arr = bubble_sort(arr)

puts sort_arr.inspect
